spring:
  profiles:
    active: native
  application:
    name: config-server
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config
server:
  port: 8782
eureka:
  client:
    service-url:
      default: http://localhost:8761/eureka/